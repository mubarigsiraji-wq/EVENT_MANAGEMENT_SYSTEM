# Use relative /api so Vite (dev) and nginx (Docker) proxy requests to the backend.
# Avoid absolute localhost URLs — they bypass the proxy and cause CORS/network errors.
VITE_API_BASE_URL=/api

# Dev-only: where Vite proxies /api requests (backend host port on your machine)
VITE_API_PROXY_TARGET=http://localhost:5002
